#ifndef GLOBAL_H
#define GLOBAL_H

#include <stdio.h>

// Declare global variables properly with types
 extern int a;
 extern int b;
 extern int c;
 extern int d;
//
 // Declare functions
 int add();
 int sub();
 int mul();
 int divide();  // (not div, because div is a math.h function)

 #endif
